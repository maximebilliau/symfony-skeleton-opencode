services:
  php:
    build:
      context: docker/php
      dockerfile: Dockerfile
    container_name: skeleton_php
    volumes:
      - ./:/var/www/skeleton
    depends_on:
      - db
    networks:
      - skeleton-network

  db:
    image: postgres:15
    container_name: skeleton_db
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: skeleton
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    networks:
      - skeleton-network

  nginx:
    image: nginx:alpine
    container_name: skeleton_nginx
    volumes:
      - ./:/var/www/skeleton
      - ./docker/nginx/conf.d/:/etc/nginx/conf.d/
    labels:
        - "traefik.enable=true"
        - "traefik.http.routers.symfony.rule=Host(`skeleton.localhost`)"  # Remplacez par votre domaine
        - "traefik.http.services.symfony.loadbalancer.server.port=81"
    networks:
      - skeleton-network

  traefik:
      image: traefik:v2.10
      container_name: skeleton-traefik
      command:
          - "--api.insecure=true"
          - "--providers.docker=true"
          - "--entrypoints.web.address=:81"
      ports:
          - "81:80"
          - "8081:8080"
      volumes:
          - "/var/run/docker.sock:/var/run/docker.sock"
      networks:
          - skeleton-network

volumes:
  db_data:

networks:
    skeleton-network:
